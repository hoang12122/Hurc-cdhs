# Project-wide npm install policy.
# The application currently uses a Next/React 19 stack while several UI/runtime
# packages still publish conservative peer dependency ranges. Use npm's legacy
# peer dependency resolver consistently for CI, Docker and local verification
# until the dependency matrix is fully upgraded and locked.
legacy-peer-deps=true

# Keep engine-strict disabled unless the release process requires hard engine enforcement.
# engine-strict=true
