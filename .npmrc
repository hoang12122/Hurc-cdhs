# Project-wide npm install policy.
# The application currently targets Next 16 and React 19 while several UI/3D packages still publish narrower peer dependency metadata.
# Keep local, CI and Docker installs consistent until all upstream peers are fully aligned.
legacy-peer-deps=true
# engine-strict=true
